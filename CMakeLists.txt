cmake_minimum_required(VERSION 3.24)
project(OpenMS4Suite LANGUAGES NONE)
include(CTest)
find_package(Python3 3.12 REQUIRED COMPONENTS Interpreter)
add_test(NAME source_contracts COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tools/validate_source.py)
message(STATUS "Suite composition only: configure/install each package independently; see README.md")
